pool:
  vmImage: 'ubuntu-18.04'

resources:
  containers:
  - container: elixir
    image: elixir:1.10.4
  
steps:

- script: |
    ls
    echo "-----"
    pwd
  target: elixir

- script: |
    docker ps
  target: host